name: kWatch
options:
  bundleIdPrefix: app.kraftly
  deploymentTarget:
    macOS: "13.0"
  xcodeVersion: "15.0"

settings:
  SWIFT_STRICT_CONCURRENCY: complete
  MACOSX_DEPLOYMENT_TARGET: "13.0"
  SWIFT_VERSION: "5.9"

targets:
  kWatch:
    type: application
    platform: macOS
    sources:
      - path: .
        excludes:
          - "project.yml"
          - "**/*.md"
          - "Tests/**"
          - "UITests/**"
          - "kWatchWidget/**"
          - "kWatchWidgetTests/**"
          - "kWatchLiveActivity/**"
          - "kWatchLiveActivityTests/**"
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: app.kraftly.kwatch
      INFOPLIST_FILE: Info.plist
      CODE_SIGN_STYLE: Automatic
      DEVELOPMENT_TEAM: ""
      LSUIElement: true
    dependencies:
      - package: kFoundation
        product: MetricsKit
      - target: kWatchIntents
    preBuildScripts:
      - name: "SwiftLint"
        script: "if which swiftlint >/dev/null; then swiftlint; fi"
        basedOnDependencyAnalysis: false

  kWatchWidget:
    type: app-extension
    platform: macOS
    sources:
      - Widget
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: app.kraftly.kwatch.widget
      MACOSX_DEPLOYMENT_TARGET: "13.0"
      SWIFT_VERSION: "5.9"
    dependencies:
      - package: kFoundation
        product: MetricsKit

  kWatchLiveActivity:
    type: app-extension
    platform: macOS
    sources:
      - LiveActivity
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: app.kraftly.kwatch.liveactivity
      MACOSX_DEPLOYMENT_TARGET: "14.0"
      SWIFT_VERSION: "5.9"
    dependencies:
      - package: kFoundation
        product: MetricsKit

  kWatchIntents:
    type: app-extension
    platform: macOS
    sources:
      - Intents
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: app.kraftly.kwatch.intents
      MACOSX_DEPLOYMENT_TARGET: "13.0"
      SWIFT_VERSION: "5.9"
    dependencies:
      - package: kFoundation
        product: MetricsKit

  kWatchTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - Tests
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: app.kraftly.kwatch.tests
      MACOSX_DEPLOYMENT_TARGET: "13.0"
      SWIFT_VERSION: "5.9"
    dependencies:
      - target: kWatch
      - target: kWatchIntents
      - package: kFoundation
        product: MetricsKit

  kWatchUITests:
    type: bundle.ui-testing
    platform: macOS
    sources:
      - UITests
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: app.kraftly.kwatch.uitests
      MACOSX_DEPLOYMENT_TARGET: "13.0"
      SWIFT_VERSION: "5.9"
    dependencies:
      - target: kWatch

packages:
  kFoundation:
    path: /Users/mengjianjun/Documents/ai/aicoding/macapp/kFoundation